# 团队协作与过程留痕规范

代码实现过程同时遵循仓库根目录的 [`AGENTS.md`](AGENTS.md)。该文件规定编码前澄清、简单方案优先、精准改动、验证驱动、安全边界和项目专属实现约束；本文档侧重分支、提交、评审及过程留痕。

## 分支约定

- `main`：稳定基线，只通过 Pull Request 合并。
- `feature/<issue>-<name>`：功能开发，例如 `feature/12-agent-chat`。
- `fix/<issue>-<name>`：缺陷修复。
- `docs/<issue>-<name>`：文档与过程材料。
- `chore/<issue>-<name>`：构建、依赖、部署和维护工作。

## 提交约定

提交信息采用 Conventional Commits，并在正文关联需求或 Issue：

```text
feat(agent): add tool confirmation workflow

Requirement: FR-AGT-007
Issue: #12
Verification: pytest tests/agent/test_confirmation.py
```

推荐类型：`feat`、`fix`、`docs`、`test`、`refactor`、`perf`、`security`、`build`、`ci`、`chore`。

每次提交必须提供与变更复杂度相匹配的文字说明和过程记录：

- 标题简明说明变更类型、作用域和结果，禁止使用“修改一下”“更新代码”等含义不明的描述。
- 非简单变更应在提交正文说明背景、主要改动、影响范围和验证结果。
- 提交必须关联需求编号、Issue、Pull Request、ADR 或项目日志中的至少一项可追踪记录。
- 影响接口、数据、权限、安全、部署或验收基线的变更，必须同步更新对应文档和过程日志。

## 代码注释规范

编写代码时必须加入必要、准确且可维护的注释或文档说明，使评审者能够理解代码的目的、约束和关键决策：

- 公共类、公共接口、公共函数以及复杂模块应说明职责、输入、输出、异常和重要副作用。
- 对复杂算法、业务规则、权限边界、并发控制、事务处理、兼容逻辑和临时规避方案说明“为什么这样实现”。
- Agent 提示、工具调用、安全护栏、模型降级和数据处理逻辑应说明适用范围及失败处理方式。
- 注释必须与代码同步更新，不得保留失效、误导、重复代码字面含义或大段被注释掉的旧代码。
- 简单且含义清晰的代码无需逐行注释；注释应补充代码本身无法充分表达的信息。

## 代码结构与文件规模规范

代码必须按模块、职责和依赖关系进行结构化组织，禁止把大量互不相关的业务逻辑持续堆砌在同一个文件中：

- 一个文件原则上只承担一个清晰的主要职责；控制器、业务服务、数据访问、模型、工具、配置和测试应按边界拆分。
- 复杂流程应拆分为命名明确、输入输出清晰且便于单独测试的函数、类或子模块，避免超长函数和深层嵌套。
- 单个源代码文件超过 500 行时必须在代码评审中检查是否需要拆分；超过 800 行时原则上必须拆分，确有理由保留时需在 Pull Request 中书面说明。
- 单个函数或方法超过 60 行、嵌套层级超过 4 层时，应优先通过提取函数、策略对象、领域服务或状态机降低复杂度。
- 拆分不能破坏内聚性或形成大量无意义的微小文件；应围绕业务能力、领域边界和可独立测试性组织代码。
- 自动生成代码、数据库迁移、协议模型、静态数据和声明式配置可不受行数阈值限制，但必须标明来源，且不得手工混入核心业务逻辑。

## Pull Request 最低要求

1. 说明变更目的、范围和不包含的内容。
2. 列出关联需求编号和 Issue。
3. 说明数据、接口、权限、隐私和兼容性影响。
4. 提供执行过的测试、结果和必要截图。
5. 更新项目日志、需求追踪矩阵或 ADR（如适用）。
6. 禁止提交密钥、真实敏感数据、依赖缓存和构建产物。

## Definition of Done

- 需求与验收标准已满足。
- 自动化测试通过，必要的人工测试有记录。
- 日志、错误处理、权限与安全边界已检查。
- 公共接口和复杂逻辑具备必要且准确的注释或文档说明。
- 代码按职责结构化组织，不存在无合理说明的超长文件、超长函数或大段逻辑堆砌。
- Git 提交包含清晰的变更说明，并已关联必要的过程记录。
- 文档和追踪矩阵同步更新。
- 至少一名非作者成员完成代码评审。
