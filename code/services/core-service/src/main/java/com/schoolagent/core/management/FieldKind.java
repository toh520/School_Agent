package com.schoolagent.core.management;

/** Input kinds shared by server validation, CSV conversion and the admin form renderer. */
public enum FieldKind {
  TEXT,
  LONG_TEXT,
  INTEGER,
  DECIMAL,
  URL,
  LIST,
  SELECT
}
