# 项目过程日志

本文件从当前项目初始基线开始记录关键活动。日常代码变更以 Git 提交和 Pull Request 为准；涉及需求范围、架构、安全、验收或计划的重要事件同时记录在此。

## 2026-08-31｜S3/M03 非作者验收通过

- 验收人：夫（非作者验收）。
- 验收范围：八类资料维护、字段与关系校验、CSV 两阶段导入、有效/停用筛选、资料/账号/操作记录分页、账号状态管理、操作日志可追踪性、V5/V6 数据迁移和权限边界。
- 自动化证据：Java 26/26、pytest 5/5、隔离 Playwright 9/9、真实 Playwright 7/7；前端类型、格式和生产构建及后端打包均通过。
- 缺陷结论：无遗留 S1/S2 缺陷；Element Plus 单包体积警告为非阻断性能事项，不影响本阶段验收。
- 文档同步：README、技术报告、M03 设计、验收记录、需求追踪矩阵、测试日志和项目日志均更新为“已验收”。
- 当前状态：S3/M03 已验收，允许进入 S4/M04。

## 2026-08-31｜补齐 M03 分页、停用筛选与操作日志可读字段

- 前端补齐：资料、账号和操作记录采用每页 20 条分页；资料列表可切换有效/已停用状态，并在停用列表隐藏编辑和停用操作。
- 日志补齐：操作记录展示中文动作、操作内容、操作者账号与用户标识、资料类型与编码、请求编号和时间。
- 接口契约：重复资料编码由普通校验失败调整为 HTTP 409 冲突；操作日志响应补充安全的 `actorUsername` 字段。
- 测试覆盖：新增资料分页/停用筛选、日志字段/分页和重复编码冲突测试；完整结果为 Java 26/26、pytest 5/5、隔离 Playwright 9/9、真实 Playwright 7/7。
- 页面核对：真实数据库中已停用资料可筛选，34 条操作日志出现分页，操作者、作用对象和请求编号显示完整；保持 M04/M08 功能边界不变。

## 2026-08-31｜简化校园公告资料并对齐后续 RAG 契约

- 需求变更：校园资料改为一段可直接检索的公告文字，只维护内部编码、标题、类别、检索关键词、正文和信息来源。
- 实现调整：删除责任部门、适用对象、发布时间、有效期、版本、权限、审核和附件字段；后台导航改为“校园公告”，动态表单和 CSV 模板同步收敛。
- 数据兼容：保留已应用的 V5 迁移，新增 Flyway V6 将既有公告数据转换为类别、关键词和正文结构，避免修改历史迁移校验和人工重建数据库。
- 后续边界：M03 只保存检索语料；分块、向量化、混合检索、RAG 和大模型证据回答仍属于 M08。
- 文档影响：同步调整 FR-ADM-006、FR-QA-001～010、M03/M08 技术设计、追踪矩阵、模板和验收步骤。
- 验证证据：Java 25/25、pytest 5/5、隔离 Playwright 7/7、真实 Playwright 7/7；V6 成功迁移；真实页面确认只存在六项公告字段。

## 2026-08-31｜完成 S3/M03 统一信息资料管理开发与自测

- 关联范围：FR-ADM-001～008、AT-16、AT-18，仅开发 M03。
- 后端与数据：新增八类资料、统一 Schema/校验、CRUD/搜索/分页/逻辑删除、CSV 两阶段导入、账号状态和操作记录；Flyway V5 建表、索引、外键和脱敏种子数据。
- 前端设计：依据 `frontend-design` 建立“校园资料台账”，使用固定分类栏、资料画布、动态表单、完整度刻度和行级导入错误清单；不展示开发状态或 Demo 字样。
- 安全边界：管理接口仅允许 `INFO_ADMIN`；学生真实访问返回 403；账号接口不返回密码/令牌；敏感配置键禁止入库；知识附件只保存 URL/MIME 元数据。
- 验证证据：Java 25/25、pytest 5/5、隔离 Playwright 7/7、真实 Playwright 7/7；前端格式/类型/构建、后端打包、Flyway V6、桌面及 390px 页面检查通过。
- 回归修复：真实测试发现停用资料无法按 INACTIVE 状态核查，修正查询边界后完整复测通过。
- 文档同步：新增 M03 设计、八类 CSV 模板和验收步骤，更新 README、技术报告、追踪矩阵、测试日志及项目日志。
- 范围控制：未开发 Agent、食堂推荐、考试规划、图书推荐、附件解析检索或校园问答。
- 当前状态：S3/M03 功能开发完成，待夫进行非作者验收；通过前不得进入 S4/M04。

## 2026-08-31｜S2/M02 非作者验收通过

- 验收人：夫（非作者验收）。
- 验收范围：学生注册与登录、个人资料和偏好、四类授权、撤回清理、角色与所有权边界、审计、PostgreSQL V4 数据约束、数据库表格操作和正式页面。
- 自动化证据：Java 15/15、pytest 5/5、隔离 Playwright 5/5、真实 Playwright 4/4；前端类型、格式和生产构建通过。
- 缺陷结论：无遗留 S1/S2 缺陷；Element Plus 单包体积警告留待后续性能阶段处理，不影响本阶段验收。
- 文档同步：README、M02 验收记录、需求追踪矩阵、测试日志和项目过程日志均已更新。
- 当前状态：S2/M02 已验收，允许进入 S3/M03；S3 尚未开始。

## 2026-08-31｜完成 M02 学生注册、数据库约束与前端视觉优化

- 需求调整：学校统一认证入口改为学生注册，注册采集学号、手机号、姓名、登录账号、密码和确认密码；公开注册只创建学生角色。
- 后端实现：新增注册接口和事务服务，注册时同步创建用户、偏好及四类默认拒绝授权；注册成功后自动建立可撤销会话并写入审计。
- 数据设计：Flyway V4 增加学生身份字段、格式/非空约束、学号与手机号唯一索引、账号不区分大小写唯一索引和常用审计索引。
- 数据库工具：全局安装 VS Code Database Client，并新增 PostgreSQL 表格查看与本机安全操作指南。
- 前端设计：依据 `frontend-design` 采用“校园身份凭证台”方向，重构注册/登录入口、身份资料展示和响应式布局；页面不展示开发阶段、基础服务状态或相关测试性字样。
- 规范处理：响应式样式拆分为独立文件，保证单个源文件不超过 500 行；停止脚本处理进程退出竞态。
- 验证证据：Java 15/15、pytest 5/5、隔离 Playwright 5/5、真实 Playwright 4/4；类型检查、格式检查、生产构建、V4 迁移和完整健康链路通过。
- 当前状态：M02 调整完成，仍待非作者按更新后的验收步骤复现。

## 2026-08-30｜完成 S2/M02 身份、用户与授权实现和自测

- 关联范围：FR-IAM-001～008、AT-01、AT-15，仅开发 M02。
- 完成内容：用户/会话/偏好/授权/清理/审计模型；登录、刷新旋转、退出、资料与偏好、四类独立授权、撤回清理、所有权和角色边界；登录与个人中心页面。
- 安全决策：Java 为权限唯一可信入口；JWT 绑定可撤销服务端会话；刷新令牌只存摘要；失败与拒绝审计使用独立事务；四类敏感数据默认拒绝。
- 验证证据：Java 12/12、隔离 Playwright 4/4、真实 Playwright 3/3；AT-01、AT-15 和 Flyway V2 迁移通过。
- 前置回归：修正 M01 停止脚本在 Windows PowerShell 中的字符串匹配兼容性，并验证项目进程和数据库可正常停止。
- 范围控制：未开发 M03 及后续业务模块。
- 当前状态：M02 开发完成、待非作者验收；未满足评审门禁前不得进入 S3。
- 详细证据：`docs/design/M02-身份用户与授权设计.md`、`docs/process/M02_ACCEPTANCE.md`、`docs/process/TEST_LOG.md`。

## 2026-08-31｜完善 M02 正式页面与本地测试数据表述

- 调整内容：登录表单不再预填账号和密码；移除阶段标签、测试用途提示和基础服务状态展示；页面改用正式的身份访问与隐私说明。
- 数据调整：本地测试账号使用中性显示名称，新增 V3 迁移兼容已存在的本机数据库。
- 范围控制：后端健康接口继续用于运维与自动化检查，但不在登录页面暴露基础设施状态。
- 验证结果：仓库全量测试通过；真实 Playwright 3/3；项目自身 Flyway 成功校验 4 条迁移，数据库版本为 V3；浏览器检查确认页面无“演示/Demo”和基础服务状态文字，账号及密码均为空。

## 2026-08-30｜完成 S1/M01 工程基础实现与验收

- 背景：依据技术开发报告严格执行第一阶段，只建设后续模块共享的可重复工程基础。
- 关联范围：S1/M01、全局 NFR 基础、ADR-0001。
- 完成内容：建立 Vue、Spring Boot、FastAPI 三端工程；PostgreSQL/pgvector 与 Flyway；公共响应、错误、请求标识、时区、分页、日志脱敏和配置预检；依赖安装、测试、启动与停止脚本。
- 版本锁定：前端使用 `package-lock.json`，Python 使用 `requirements.lock`，Java 使用 Maven POM、Spring Boot BOM 与 Enforcer。
- 验证证据：JUnit 7、pytest 5、Playwright 3 个场景全部通过；真实浏览器健康链路返回 Java、Python、PostgreSQL/pgvector 全部 `UP`；Flyway 2 条迁移成功；npm audit 为 0。
- 安全检查：真实本地配置未进入 Git；日志不含本地测试密码；缺失配置快速失败；服务停止后 5173、8000、8080 和 5432 端口全部释放。
- 范围控制：未实现登录、资料后台、Agent 对话或四项核心业务功能。
- 验收结论：非作者验收人夫按 README 在 VS Code 中完成环境、测试、启停、数据库、日志、安全和范围审查；启动竞态修复后复验通过。
- 当前状态：M01 与 M01 范围内的 NFR 基础均标记“已验收”，允许进入 S2/M02；S2 尚未开始。
- 详细记录：`docs/process/M01_ACCEPTANCE.md`、`docs/process/TEST_LOG.md`。
- 配置治理：新增 `docs/design/M01-配置项与环境变量清单.md`，登记数据库与三端地址、敏感性、读取方式和新增配置流程，作为后续禁止硬编码的依据。
- 目录治理：按统一代码入口要求，将前端、后端、部署配置、跨模块测试和工程脚本归并到 `code/`，同步修正脚本根路径、README、技术报告、配置清单、测试记录和追踪矩阵；`docs`、`logs` 与 `tmp` 继续按各自职责保留在仓库根目录。
- 启动修复：修正聚合健康接口返回 `DOWN` 时启动脚本无等待便立即重试的竞态，确保每次未就绪后等待 1 秒；真实链路和停止流程回归通过。
- 配置迁移：将部署配置从根目录 `deploy` 迁入 `code/deploy`，同步脚本默认读取路径和文档；全量测试、真实健康链路与启停回归通过，本地 `.env.local` 继续保持 Git 忽略。

## 2026-08-28｜完成开发环境审查与基础工具安装

- 背景：依据需求分析和技术开发报告审查本机开发环境，并补齐当前 V1.0 本地开发必需的基础工具。
- 隔离策略：新建独立 Conda 环境 `school-agent`，不修改 Miniconda base 和已有 `rgbt` 科研环境。
- 安装内容：Python 3.12.13、pip 26.2、pytest 9.1.1、Ruff 0.16.1、OpenJDK 21.0.10 LTS、Maven 3.9.16、PostgreSQL 16.14 和 pgvector 0.8.3；同时补充 Java、Spring Boot、Vue 和 YAML 的 VS Code 扩展。
- 兼容性决策：PostgreSQL 17 与当前 Windows pgvector Conda 构建存在依赖冲突，改用已兼容且满足项目要求的 PostgreSQL 16.14；失败事务未创建残留环境。
- 验证证据：全部命令行工具已完成版本核验；一次性 PostgreSQL 实例成功加载 pgvector 0.8.3 并完成向量距离计算，随后已停止实例并删除测试数据目录。
- 范围控制：Docker Desktop、WSL2、Nginx 属于可选部署环境，本次不安装；项目框架依赖将在对应单模块开发阶段通过锁定依赖文件安装。
- 记录文件：`docs/开发环境审查与安装记录.txt`。
- 待办：S1 阶段创建正式的本地 PostgreSQL 数据目录、项目数据库和环境变量示例，不在环境审查阶段预设真实口令或常驻服务。

## 2026-08-28｜技术开发报告调整为功能模块化路线

- 背景：原技术报告虽定义 M01～M10，但开发阶段按架构、业务、AI 和前端横向推进，不便于按功能模块逐项开发和验收。
- 完成内容：依据 V1.0 需求域重写模块说明，明确 M01～M10 的功能范围、依赖、纵向交付物和退出条件。
- 开发方法：除工程基础外，每个模块按需求确认、设计契约、确定性业务、Agent 能力、前端与端到端验证、验收留痕六个阶段闭环。
- 里程碑：按 M01、M02、M03/M04、M05～M08、M09/M10 的依赖顺序推进，四个核心业务模块分别形成可独立演示的纵向切片。
- 追踪更新：需求追踪矩阵已补充各需求域对应的技术开发报告模块。
- 串行约束：开发阶段调整为 S1～S10，每个阶段只允许开发一个对应模块；M03 在本阶段一次性完成全部资料管理，M09 与 M10 分别独立实施，不再与其他模块并行开发。
- 验证：检查需求编号范围、模块依赖、AT-01～18 映射、MVP 边界和 Markdown 结构。

## 2026-08-27｜确定 V1.0 课设范围

- 需求基线：将当前 Markdown 定义为 V1.0。
- 管理方式：合并为一个信息资料管理员和一个统一管理后台，集中维护全部基础信息，并强调录入完整度。
- 考试记录：只录入科目、考试时间和备注，由系统自动排序并展示最近考试，不调用 AI。
- AI 规划：学生主动请求后，AI 再进行必要追问并调用考试、资料、书籍和计划计算工具，生成基础复习计划书。
- MVP 边界：实现计划书生成、保存、查看、删除和重新生成；任务勾选、进度跟踪与动态重排作为后续扩展。
- 运行方式：开发阶段仅要求本地测试和课堂演示，个人云服务器部署可选，不建设工业级集群、复杂监控和容灾体系。
- 验证：检查需求编号、角色一致性、考试字段、MVP 边界、部署描述和活动文档同步情况。

## 2026-08-27｜建立项目初始基线

- 项目范围：面向学生的智慧校园 AI 助手。
- 基础能力：登录、账户、个人偏好、授权、权限和审计。
- 核心功能：智能食堂推荐、考试安排与 AI 学习规划、学校书籍查询推荐、校园知识问答。
- 正式需求：`docs/requirements/智慧校园智能体系统需求分析报告.md`。
- 协作规则：代码应包含必要注释并按职责结构化组织；Git 提交应记录背景、主要变更、影响和验证结果。
- 初始状态：当前仓库文件作为项目开发起点，不包含早期需求版本、生成工具或历史过程记录。
- 验证要求：检查 Markdown 结构、需求编号、仓库链接、Git 差异和工作区状态后建立根提交。

## 记录模板

### YYYY-MM-DD｜事件标题

- 背景：
- 参与者：
- 关联 Issue/需求：
- 完成内容：
- 决策与原因：
- 验证证据：
- 风险与待办：
