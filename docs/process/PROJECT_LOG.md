# 项目过程日志

本文件从当前项目初始基线开始记录关键活动。日常代码变更以 Git 提交和 Pull Request 为准；涉及需求范围、架构、安全、验收或计划的重要事件同时记录在此。

## 2026-08-30｜完成 S1/M01 工程基础实现与验收

- 背景：依据技术开发报告严格执行第一阶段，只建设后续模块共享的可重复工程基础。
- 关联范围：S1/M01、全局 NFR 基础、ADR-0001。
- 完成内容：建立 Vue、Spring Boot、FastAPI 三端工程；PostgreSQL/pgvector 与 Flyway；公共响应、错误、请求标识、时区、分页、日志脱敏和配置预检；依赖安装、测试、启动与停止脚本。
- 版本锁定：前端使用 `package-lock.json`，Python 使用 `requirements.lock`，Java 使用 Maven POM、Spring Boot BOM 与 Enforcer。
- 验证证据：JUnit 7、pytest 5、Playwright 3 个场景全部通过；真实浏览器健康链路返回 Java、Python、PostgreSQL/pgvector 全部 `UP`；Flyway 2 条迁移成功；npm audit 为 0。
- 安全检查：真实本地配置未进入 Git；日志不含本地测试密码；缺失配置快速失败；服务停止后 5173、8000、8080 和 5432 端口全部释放。
- 范围控制：未实现登录、资料后台、Agent 对话或四项核心业务功能。
- 验收结论：非作者验收人夫按 README 在 VS Code 中完成环境、测试、启停、数据库、日志、安全和范围审查；启动竞态修复后复验通过。
- 当前状态：M01 与 M01 范围内的 NFR 基础均标记“已验收”，允许进入 S2/M02；S2 尚未开始。
- 详细记录：`docs/process/M01_ACCEPTANCE.md`、`docs/process/TEST_LOG.md`。
- 配置治理：新增 `docs/design/M01-配置项与环境变量清单.md`，登记数据库与三端地址、敏感性、读取方式和新增配置流程，作为后续禁止硬编码的依据。
- 目录治理：按统一代码入口要求，将前端、后端、跨模块测试和工程脚本归并到 `code/`，同步修正脚本根路径、README、技术报告、配置清单、测试记录和追踪矩阵；`deploy`、`docs`、`logs` 与 `tmp` 继续按各自职责保留在仓库根目录。
- 启动修复：修正聚合健康接口返回 `DOWN` 时启动脚本无等待便立即重试的竞态，确保每次未就绪后等待 1 秒；真实链路和停止流程回归通过。

## 2026-08-28｜完成开发环境审查与基础工具安装

- 背景：依据需求分析和技术开发报告审查本机开发环境，并补齐当前 V1.0 本地开发必需的基础工具。
- 隔离策略：新建独立 Conda 环境 `school-agent`，不修改 Miniconda base 和已有 `rgbt` 科研环境。
- 安装内容：Python 3.12.13、pip 26.2、pytest 9.1.1、Ruff 0.16.1、OpenJDK 21.0.10 LTS、Maven 3.9.16、PostgreSQL 16.14 和 pgvector 0.8.3；同时补充 Java、Spring Boot、Vue 和 YAML 的 VS Code 扩展。
- 兼容性决策：PostgreSQL 17 与当前 Windows pgvector Conda 构建存在依赖冲突，改用已兼容且满足项目要求的 PostgreSQL 16.14；失败事务未创建残留环境。
- 验证证据：全部命令行工具已完成版本核验；一次性 PostgreSQL 实例成功加载 pgvector 0.8.3 并完成向量距离计算，随后已停止实例并删除测试数据目录。
- 范围控制：Docker Desktop、WSL2、Nginx 属于可选部署环境，本次不安装；项目框架依赖将在对应单模块开发阶段通过锁定依赖文件安装。
- 记录文件：`docs/开发环境审查与安装记录.txt`。
- 待办：S1 阶段创建正式的本地 PostgreSQL 数据目录、项目数据库和环境变量示例，不在环境审查阶段预设真实口令或常驻服务。

## 2026-08-28｜技术开发报告调整为功能模块化路线

- 背景：原技术报告虽定义 M01～M10，但开发阶段按架构、业务、AI 和前端横向推进，不便于按功能模块逐项开发和验收。
- 完成内容：依据 V1.0 需求域重写模块说明，明确 M01～M10 的功能范围、依赖、纵向交付物和退出条件。
- 开发方法：除工程基础外，每个模块按需求确认、设计契约、确定性业务、Agent 能力、前端与端到端验证、验收留痕六个阶段闭环。
- 里程碑：按 M01、M02、M03/M04、M05～M08、M09/M10 的依赖顺序推进，四个核心业务模块分别形成可独立演示的纵向切片。
- 追踪更新：需求追踪矩阵已补充各需求域对应的技术开发报告模块。
- 串行约束：开发阶段调整为 S1～S10，每个阶段只允许开发一个对应模块；M03 在本阶段一次性完成全部资料管理，M09 与 M10 分别独立实施，不再与其他模块并行开发。
- 验证：检查需求编号范围、模块依赖、AT-01～18 映射、MVP 边界和 Markdown 结构。

## 2026-08-27｜确定 V1.0 课设范围

- 需求基线：将当前 Markdown 定义为 V1.0。
- 管理方式：合并为一个信息资料管理员和一个统一管理后台，集中维护全部基础信息，并强调录入完整度。
- 考试记录：只录入科目、考试时间和备注，由系统自动排序并展示最近考试，不调用 AI。
- AI 规划：学生主动请求后，AI 再进行必要追问并调用考试、资料、书籍和计划计算工具，生成基础复习计划书。
- MVP 边界：实现计划书生成、保存、查看、删除和重新生成；任务勾选、进度跟踪与动态重排作为后续扩展。
- 运行方式：开发阶段仅要求本地测试和课堂演示，个人云服务器部署可选，不建设工业级集群、复杂监控和容灾体系。
- 验证：检查需求编号、角色一致性、考试字段、MVP 边界、部署描述和活动文档同步情况。

## 2026-08-27｜建立项目初始基线

- 项目范围：面向学生的智慧校园 AI 助手。
- 基础能力：登录、账户、个人偏好、授权、权限和审计。
- 核心功能：智能食堂推荐、考试安排与 AI 学习规划、学校书籍查询推荐、校园知识问答。
- 正式需求：`docs/requirements/智慧校园智能体系统需求分析报告.md`。
- 协作规则：代码应包含必要注释并按职责结构化组织；Git 提交应记录背景、主要变更、影响和验证结果。
- 初始状态：当前仓库文件作为项目开发起点，不包含早期需求版本、生成工具或历史过程记录。
- 验证要求：检查 Markdown 结构、需求编号、仓库链接、Git 差异和工作区状态后建立根提交。

## 记录模板

### YYYY-MM-DD｜事件标题

- 背景：
- 参与者：
- 关联 Issue/需求：
- 完成内容：
- 决策与原因：
- 验证证据：
- 风险与待办：
